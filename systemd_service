#Place in /etc/systemd/system/
#Update the path and arguments then enable and start the service.

[Unit]
Description=Start the monitoring api Python script
After=network.target

[Service]
Type=simple
ExecStart=python3 /PATH/TO/cosmos_validators_monitoring.py -i VALIDATOR_NAME PORT -i VALIDATOR_NAME2 PORT2 etc.

[Install]
WantedBy=multi-user.target
